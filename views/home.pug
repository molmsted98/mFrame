extends layout

block content
  h1 Hackathon Starter
  p.lead A boilerplate for Node.js web applications.
  hr
  .row
    each user in users
      if (typeof(user.profile) !== 'undefined')
        h1
        a(href='/userProfile/' + user._id)= user.profile.name
